// control de mayusculas (tolowerCase) y minusculas (toupercase)
//conversion de tipos de datos
//tipos de datos diferentes
// espacios vacios

let age = parseInt()(prompt("ingresa tu edad"))
let country = prompt (" ingresa tu pais").toLowerCase()

if (age >= 18  && country != "china") {
   alert ("se ha desbloqueado el catalogo de pelis violentas") 
} else {
   alert("no tiene permisos para ver estas peliculas")
}

console.log(age,country)
if (age == 0 || country =="") {
    alert("no ingresaste uno de los valores")    
} else {
    
}
/**
 * *> indicar que esto es mayor a algo
 *  *< indicar que es menor que                     "NUMEROS"
 * == comparar si un valor es igual al otro
 * <= si es menor o igual
 * 
 * 
 * != diferente
 * operadores logicos                              "CONECTORES"
 * %% 
 * ||: o
 */
